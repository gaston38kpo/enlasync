# Skill Registry — EnlaSync

Generated by sdd-init on 2026-05-12.

## Active Project Skills

None detected.

## Active User Skills

### branch-pr
- **Trigger:** creating, opening, or preparing PRs for review.
- **Path:** `C:\Users\ggiacobini\.config\opencode\skills\branch-pr\SKILL.md`
- **Compact Rules:**
  - Every PR MUST link an approved issue.
  - Every PR MUST have exactly one `type:*` label.
  - Branch names MUST match `^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)\/[a-z0-9._-]+$`.
  - Commit messages MUST match conventional commits regex.
  - Automated checks must pass before merge.

### chained-pr
- **Trigger:** PRs over 400 lines, stacked PRs, review slices.
- **Path:** `C:\Users\ggiacobini\.config\opencode\skills\chained-pr\SKILL.md`
- **Compact Rules:**
  - Split PRs over 400 changed lines unless `size:exception` is accepted.
  - Keep each PR reviewable in ≤60 minutes.
  - One deliverable work unit per PR; keep tests/docs with the unit.
  - State start, end, dependencies, follow-up, and out-of-scope in every chained PR.
  - Every child PR must include a dependency diagram marking current with `📍`.

### cognitive-doc-design
- **Trigger:** writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs.
- **Path:** `C:\Users\ggiacobini\.config\opencode\skills\cognitive-doc-design\SKILL.md`
- **Compact Rules:**
  - Lead with the answer; context comes after.
  - Progressive disclosure: happy path first, then details/edge cases.
  - Chunking: group related info into small sections.
  - Signposting: headings, labels, callouts, summaries.
  - Recognition over recall: tables, checklists, examples over prose.
  - Review empathy: reviewers verify intent without reconstructing the whole story.

### comment-writer
- **Trigger:** PR feedback, issue replies, reviews, Slack messages, or GitHub comments.
- **Path:** `C:\Users\ggiacobini\.config\opencode\skills\comment-writer\SKILL.md`
- **Compact Rules:**
  - Start with the actionable point; do not recap the whole PR.
  - Be warm and direct; sound like a thoughtful teammate.
  - Prefer 1–3 short paragraphs or a tight bullet list.
  - Give the technical reason when asking for a change.
  - Comment on the highest-value issue, not every tiny preference.
  - Match thread language (Rioplatense Spanish/voseo when writing in Spanish).
  - No em dashes.

### issue-creation
- **Trigger:** creating GitHub issues, bug reports, or feature requests.
- **Path:** `C:\Users\ggiacobini\.config\opencode\skills\issue-creation\SKILL.md`
- **Compact Rules:**
  - Blank issues are disabled; MUST use a template.
  - Every issue gets `status:needs-review` automatically.
  - A maintainer MUST add `status:approved` before any PR can be opened.
  - Questions go to Discussions, not issues.

### judgment-day
- **Trigger:** judgment day, dual review, adversarial review, juzgar.
- **Path:** `C:\Users\ggiacobini\.config\opencode\skills\judgment-day\SKILL.md`
- **Compact Rules:**
  - Resolve project skills before launching agents.
  - Launch two blind judges in parallel; never review code yourself.
  - Wait for both judges before synthesis; never accept partial verdicts.
  - Classify warnings as `WARNING (real)` only if normal use can trigger them.
  - Ask before fixing Round 1 confirmed issues.
  - After any fix agent runs, immediately re-launch both judges in parallel.
  - Terminal states: `JUDGMENT: APPROVED` or `JUDGMENT: ESCALATED`.

### react-doctor
- **Trigger:** Run after making React changes to catch issues early.
- **Path:** `C:\Users\ggiacobini\.gemini\skills\react-doctor\SKILL.md`
- **Compact Rules:**
  - Run `npx -y react-doctor@latest . --verbose --diff` after making React changes.
  - Fix errors first, then re-run to verify the score improved.

### skill-creator
- **Trigger:** new skills, agent instructions, documenting AI usage patterns.
- **Path:** `C:\Users\ggiacobini\.config\opencode\skills\skill-creator\SKILL.md`
- **Compact Rules:**
  - Follow `docs/skill-style-guide.md` if it exists; otherwise use inline fallback rules.
  - A skill is a runtime instruction contract for an LLM, not human documentation.
  - Do not add a `Keywords` section; preserve essential trigger words in `description`.
  - References must point to local files.
  - Keep skill body concise: target 180–450 tokens, hard max 1000.
  - Use required frontmatter: name, description, license, metadata.author, metadata.version.
  - Write sections in order: Activation Contract, Hard Rules, Decision Gates, Execution Steps, Output Contract, References.

### work-unit-commits
- **Trigger:** implementation, commit splitting, chained PRs, or keeping tests and docs with code.
- **Path:** `C:\Users\ggiacobini\.config\opencode\skills\work-unit-commits\SKILL.md`
- **Compact Rules:**
  - A commit represents a deliverable behavior, fix, migration, or docs unit.
  - Do not commit by file type; avoid `models`, then `services`, then `tests`.
  - Keep tests with code; docs with the user-visible change they explain.
  - Tell a story: reviewer should understand why each commit exists.
  - Future PR-ready: each commit should be a candidate chained PR.
  - If SDD tasks forecast >400-line change, group commits into chained PR slices before implementation.

## Project Conventions

No convention files detected (no AGENTS.md, .cursorrules, etc.).
